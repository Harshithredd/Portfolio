const { render, screen } = require("@testing-library/react")
const { default: About } = require("../About");


describe('Aout Page',()=>{
    it('Render Aout Page',()=>{
        // render(<About/>);
        // const aboutTitle = screen.getByText('Passion Fuels Purpose!')
        // expect(aboutTitle).toBeInTheDocument();
    })
})